import { GatedMetric, type MetricResult } from "./GatedMetric";
export function DriftPanel({ data, asOf }: { data: { overall: MetricResult; threshold: number; threshold_label: string }; asOf: string }) { const threshold: MetricResult = { status: "MEASURED", value: data.threshold, ci_low: null, ci_high: null, n: data.overall.n, minimum_n: 1, reason: null }; return <section id="drift" className="rounded border border-border bg-surface p-5"><h2 className="text-heading font-semibold">Feature drift</h2><p className="text-xs text-muted">Data as of {asOf}</p><p><GatedMetric metric={data.overall} /></p><p className="text-sm text-muted">PSI alert threshold <GatedMetric metric={threshold} />: {data.threshold_label}.</p></section>; }
