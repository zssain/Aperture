import { GatedMetric, type MetricResult } from "./GatedMetric";
export function DisparityPanel({ data, asOf }: { data: Record<string, Record<string, MetricResult>>; asOf: string }) { const cells = Object.entries(data).flatMap(([attribute, groups]) => Object.entries(groups).map(([group, metric]) => ({ attribute, group, metric }))); return <section id="disparity" className="rounded border border-border bg-surface p-5"><h2 className="text-heading font-semibold">Disparity</h2><p className="text-xs text-muted">Data as of {asOf}</p>{cells.length ? <div className="overflow-x-auto"><table className="w-full"><thead><tr><th>Attribute</th><th>Subgroup</th><th>Outcome rate</th></tr></thead><tbody>{cells.map((cell) => <tr key={`${cell.attribute}-${cell.group}`}><th>{cell.attribute}</th><td>{cell.group}</td><td><GatedMetric metric={cell.metric} /></td></tr>)}</tbody></table></div> : <p className="text-neutral"><strong>NOT YET MEASURABLE</strong> — no consented fairness attributes have enough closed outcomes.</p>}</section>; }
