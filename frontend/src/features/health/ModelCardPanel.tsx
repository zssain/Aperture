import { Button } from "../../components/ui/Button";
export function ModelCardPanel({ asOf }: { asOf: string }) { return <section id="model-card" className="rounded border border-border bg-surface p-5"><h2 className="text-heading font-semibold">Model card</h2><p className="text-xs text-muted">Data as of {asOf}</p><p className="my-3 text-sm">Generated only from the feature registry and structurally measured metrics. Unavailable metrics appear under “Not yet measured”.</p><Button onClick={() => { window.location.href = "/api/v1/health-metrics/model-card.pdf"; }}>Export model card</Button></section>; }
